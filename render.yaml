# Render 部署配置
services:
  - type: web
    name: ziwei-ai
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
